# 質疑応答 7/28

**対象:** skills-maker / skills-pack へのスキル追加と `promote-skill`
**日付:** 2026-07-28
**参加者:** ヒデさん / Tom
**目的:** 別PCでも続きできるよう、結論・決定・残タスクを残す（Cursorチャットは端末ローカルのため）

---

## Q1. 他プロジェクトの skills を skills-pack にどう載せるか

### 結論

正規ルートは `~/.cursor/skills/` に置いてから `scripts/sync-skills-pack.ps1` で pack へ同期。今回それに加え、作成直後の2段確認 companion `/promote-skill` を実装した。

### 事実

- 正本の流れ: global（`~/.cursor/skills/`）→ sync → `skills-pack/`
- マーケは別箱 `skills-pack-marketing/`（通常 install では入らない）
- `sync-skills-pack.ps1` は global **全件**ミラー（実験スキル混入リスクあり）
- `/promote-skill` は **1スキルだけ** pack へコピー + `MANIFEST.json` 更新

### 実務 / 次アクション

1. 一括ミラーが必要なら配布元PCで `.\scripts\sync-skills-pack.ps1`
2. 作成直後の載せ方は `/promote-skill` を使う

---

## Q2. promote-skill の確認フローとパス未検出時

### 結論

確認は **2段**。パスが無ければ聞いて、検証前は絶対に書かない。

### 事実

```text
作成完了
  → Gate1「グローバル（~/.cursor/skills/）に入れる？」
      No  → 終了（Gate2 出さない）
      Yes → global 配置
  → Gate2「skills-pack に同期する？」
      No  → global のみ
      Yes → skills-maker パス解決 → 検証OKのときだけ同期
```

パス解決順:

1. 会話で渡されたパス
2. 環境変数 `SKILLS_MAKER_ROOT`（検証OK時のみ）
3. `C:\Dev-App\skills-maker`（**存在するときだけ**）
4. だめなら「skills-maker のパスは？」と聞く

検証条件（`$ROOT`）:

- `$ROOT` が既存ディレクトリ
- `$ROOT/skills-pack/` が既存
- マーカーのいずれか: `install.ps1` / `install.sh` / `MANIFEST.json` / `引き継ぎ.md` / `INSTALL.md`

不合格時は **ルートも pack も新規作成しない・書かない**。

### 補足（推測は確信度つき）

- Gate2 は Gate1 Yes のときだけ、が合意（確信度: 高）

---

## Q3. create-skill / skill-creator での自動発火

### 結論

OSイベントでの自動発火は無い。`/skill-creator` は締めに handoff を書いた。Cursor 組み込み `/create-skill`（`skills-cursor`）は触れないので保証なし。

### 事実

| 経路 | 挙動 |
|------|------|
| `/skill-creator` | 末尾に「完了後は `promote-skill` へ」と追記済み。手順を最後まで従えば2段確認を出す想定 |
| `/create-skill`（skills-cursor） | 改変しない。pack 同期はしない。`promote-skill` の description 頼み |
| フック | スキル作成検知フックは無い |

### 実務 / 次アクション

| 目的 | 必要か |
|------|--------|
| このPCだけで使う（global） | create-skill で global 配置すれば promote **不要** |
| skills-pack / 他PC配布 | **promote が必要**（明示 `/promote-skill` が手堅い） |
| 強制したい | User Rule に「スキルをディスクに書いたら必ず promote-skill の2段確認」を1行 |

### 未決

- User Rule で強制するか（まだ未実施）
- create-skill 経路の発火が弱いか実運用で見る

---

## Q4. 実装したもの（このセッション）

### 結論

`/promote-skill` を skills-pack に追加し、このPCの global にも入れた。件数 53→54。

### 事実

| パス | 内容 |
|------|------|
| `skills-pack/promote-skill/SKILL.md` | companion 本体 |
| `skills-pack/promote-skill/scripts/promote-to-pack.ps1` | Windows 同期（検証付き） |
| `skills-pack/promote-skill/scripts/promote-to-pack.sh` | macOS/Linux 用 |
| `skills-pack/skill-creator/SKILL.md` | 完了後 handoff 追記 |
| `~/.cursor/skills/promote-skill/` | グローバル導入済み |
| `skills-pack/MANIFEST.json` | promote-skill 含む 54件 |
| `skills一覧.md` / `引き継ぎ.md` / `INSTALL.md` | 件数・説明を 54 に更新 |

安全テスト: 存在しないルート・誤ルート（`C:\Dev-App`）は exit 1・書き込みなし。

### 実務 / 次アクション

1. 別PC: `skills-pack/install.ps1`（または `引き継ぎ.md`）で 54件を入れる
2. 必要ならこの変更を **commit / push**（このセッションでは未 commit）
3. 実運用で create-skill / skill-creator 後に Gate が出るか確認

### 未決

- git commit / push するか（ユーザー指示待ち）

---

## 現状の合意（7/28時点）

| 項目 | 内容 |
|------|------|
| 確認 | Gate1 global → Gate2 pack sync（まとめない） |
| 同期単位 | 1スキルのみ（全件ミラーは別用途） |
| パス無し | 聞いて止める。誤場所に書かない・作らない |
| create-skill | 触れない。pack 要るときだけ promote |
| skill-creator | 締めに promote handoff あり（100%保証ではない） |
| commit | まだしない（指示待ち） |

---

## 参照

- `skills-pack/promote-skill/SKILL.md` … companion 仕様
- `skills-pack/promote-skill/scripts/promote-to-pack.ps1` … 安全な単体同期
- `scripts/sync-skills-pack.ps1` … global 全件ミラー（配布元向け）
- `skills-pack/skill-creator/SKILL.md` … After the skill is done handoff
- `skills-pack/引き継ぎ.md` … 別PCセットアップ／配布元更新
- `skills一覧.md` … 54件カタログ
