# 質疑応答 7/29

**対象:** skills-maker — スキル重複解消・agents一本化・pack/一覧整備
**日付:** 2026-07-29
**参加者:** ヒデさん / Tom
**目的:** 別PCでも続きできるよう、結論・決定・残タスクを残す（Cursorチャットは端末ローカルのため）

---

## Q1. スキル重複・件数ズレの原因と方針

### 結論

Cursor は `~/.agents/skills` と `~/.cursor/skills`（＋組み込み）を両方読む。設定画面は名前で重複排除するが `/` メニューはしないため件数がズレ、同名が2回出る。

**Include Third-Party トグルを OFF にしても `~/.agents` は読まれ続ける**（ベンダー中立の標準パスとしてネイティブ扱い）。`.agents` 由来の重複はトグルでは直らない。

### 事実

- 当時: `.cursor/skills` 約55、`.agents/skills` 約43（ほぼ重複）、組み込み約20
- 同名衝突時は `.agents` 側が勝つ観測あり → `.cursor` 側の編集が効かない事故が起きうる
- ChatGPT / Codex は `.agents` を使うため、`.agents` は消せない

### 実務 / 合意した配置（ハイブリッド）

| root | 中身 |
|------|------|
| `~/.agents/skills/<name>/` | 通常スキル全部（**平置き**・カテゴリフォルダなし） |
| `~/.cursor/skills/<name>/` | Cursor固有3のみ: `chat-handoff` / `skill-creator` / `promote-skill` |
| `~/.cursor/skills-cursor/` | 組み込み。**触らない** |

- `using-superpowers` は **`.agents` に置く**（Cursor + Codex 両方で効かせる）
- Office系（docx/xlsx/pptx/pdf）は global 維持（Cursorでも使う）
- 名前統一: `react-best-practices`（旧 vercel-…）、`notebooklm`（旧 notebooklm-skill）
- 平置き理由: Codex がネストを辿る保証がない

### 未決だったが決まったこと

- superpowers の入れ子 → **フラット**で `.agents` へ
- Office系を外す案 → **残す**（Excel読ませた実績あり）

---

## Q2. 実装・pack反映・git修復・最終整備

### 結論

このPCでは移行・pack反映・GitHub同期・ドキュメント穴埋め・残骸掃除まで完了。別PCは `git pull` → `skills-pack/install.ps1` で揃う。

### 事実

**移行結果（このPC）**

- agents **52** / cursor **3** / 重複 **0** / pack↔global **drift 0**
- `~/.cursor/skills` は固有3つだけ（空の `debug`/`github`/`playbooks`/`superpowers` と `chat-handoff-workspace` は掃除済み）
- `notebooklm` のネスト `.git` は削除済み

**pack / インストーラ**

- `install.ps1` / `install.sh`: agents 平置き＋Cursor固有3振り分け、旧 `.cursor` 残骸は `skills.bak` へ退避
- `install-claude.ps1`: **`.agents` に書かない**（以前は書いて除外10を消す構造で、移行を巻き戻す地雷だった → 修正済み）
- MANIFEST: 55件、`installTarget` 付き
- 新規: `scripts/generate-manifest.ps1` / `check-pack-drift.ps1`、`sync-skills-pack.ps1` 書き直し

**git**

- ローカル `バグ修正` と別PC `別PC上での作業と同期完了` が diverged → merge 完了・push 済み
- merge 時: docs衝突は agents 正本側を採用、`notebooklm-skill` → `notebooklm` にリネームして取り込み

**ドキュメント最終直し**

- `skills一覧.md` に欠けてた `/chat-handoff` を追加（§5・11件）。一覧↔MANIFEST **55=55**
- `SETUP.md` の古い `skills/` パス → `skills-pack/`
- `INSTALL.md` Anthropic（8）→ Anthropic系（5）＋Cursor固有3を明示

### 補足（推測）

- Office系の Python ライブラリ（`python-docx` / `openpyxl` / `python-pptx`）はこのPCでは未インストールだった。xlsxを読めた記憶があるなら別手段の可能性（確信度: 中）
- マーケパックはまだ `~/.cursor` カテゴリフォルダ方式のまま（日常パックとは別系統）（確信度: 高）

### 実務 / 次アクション（別PC）

1. `cd <skills-maker>` → `git pull`
2. `cd skills-pack` → `.\install.ps1`（必要なら `.\install-claude.ps1` も）
3. Cursor 再起動 → **設定画面の件数と `/` メニュー件数が一致**するか確認（重複ゼロがゴール）
4. Officeを使うなら: `pip install python-docx openpyxl python-pptx`
5. 新規スキル作成後は `/promote-skill`（宛先は原則 `.agents`、Cursor固有のみ `.cursor`。**両方に置かない**）

### 未決

- なし（方針・実装ともこのセッションで閉じた）
- マーケパックの agents 平置き化はスコープ外のまま

---

## 現状の合意（7/29時点）

| 項目 | 内容 |
|------|------|
| 正本 root | `~/.agents/skills`（平置き） |
| Cursor固有 | `chat-handoff` / `skill-creator` / `promote-skill` のみ `~/.cursor/skills` |
| 件数 | 日常パック 55（agents 52 + cursor 3） |
| Third-Partyトグル | `.claude` 由来には効く / `.agents` には効かない |
| 配布 | `skills-pack/install.ps1`（Claude Code は `install-claude.ps1`・`.agents`非接触） |
| 重複禁止 | 同じスキルを `.agents` と `.cursor` の両方に置かない |

---

## 参照

- `skills-pack/INSTALL.md` … 配置構成・トラブルシュート
- `skills-pack/引き継ぎ.md` … 別PCセットアップ手順
- `skills-pack/skills重複処理.md` … 重複時の整理手順（トグル誤解を明記）
- `skills一覧.md` … 日常55＋マーケ別紙
- `scripts/check-pack-drift.ps1` … pack↔global差分確認
- `scripts/generate-manifest.ps1` … MANIFEST再生成
- `~/.cursor/skills.bak/` … 退避済み旧スキル（すぐ消さなくてよい）
