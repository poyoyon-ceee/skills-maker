interface:
  display_name: "model-router-gpt"
  short_description: "CodexのGPTモデルで大量探索を分担し、必要な原文を確認"
  default_prompt: "$model-router-gpt を使い、候補を通常ツールで絞ってモデルを分担し、重要原文を確認してRouting Reportを返してください。"
policy:
  allow_implicit_invocation: false
